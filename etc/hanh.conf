#!/hint/sh
# General variables 
BIN="sysroot/prefix/bin/hanh"
SYSROOT=sysroot
MIRRORDIR=CONFDIR/hanhmirror.d
DOWNLOADER="wget -nc --no-check-certificate --quiet"

# Directories for building
hanhdir=$(pwd)
workdir=$hanhdir/work
pkgdir=$hanhdir/pkg

# Build flags
# USE_FLAGS="CFLAGS CXXFLAGS LDFLAGS"
